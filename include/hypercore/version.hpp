#pragma once

// ─────────────────────────────────────────────────────────────────────────────
// version.hpp — HyperCore version constants
// ─────────────────────────────────────────────────────────────────────────────

namespace hypercore {

inline constexpr int VERSION_MAJOR = 2;
inline constexpr int VERSION_MINOR = 6;
inline constexpr int VERSION_PATCH = 0;

inline constexpr const char* VERSION_STRING = "2.6.0";
inline constexpr const char* PROJECT_NAME   = "HyperCore";

} // namespace hypercore
